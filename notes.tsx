
// handleChange = function from parent fro redirection 
// history.push here is the handleChange fun
// buildSubmitData = function of the form

// <button onClick={async ()=>{ 
//     const submitData = handleSubmit;
//     await handleChange(); } }>Done
// </button>

// <Button onClick={async ()=>{ 
//     const submitData = this.buildSubmitData();
//     await ApiService.submitForm(submitData);
//     this.props.history.push(‘/where to’);
// }} >Submit</Button>